service: rss

image: daliusd1982/rss

servers:
  web:
    - 195.201.143.78

proxy:
  ssl: true
  hosts:
    - rss.ffff.lt
  app_port: 8080
  healthcheck:
    path: /health
    interval: 3
    timeout: 30

registry:
  server: localhost:5555

builder:
  arch: amd64

env:
  clear:
    MAIL_USER: haiku.lt.qa@gmail.com
  secret:
    - MAIL_PASS
